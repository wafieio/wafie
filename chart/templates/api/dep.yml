apiVersion: apps/v1
kind: Deployment
metadata:
  name: wafie-api
  namespace: {{.Release.Namespace}}
  labels:
    app: wafie-api
spec:
  selector:
    matchLabels:
      app: wafie-api
  template:
    metadata:
      labels:
        app: wafie-api
    spec:
      serviceAccountName: wafie-api
      containers:
        - name: api-server
          image: {{.Values.api.image}}
          imagePullPolicy: Always
          command:
            - /usr/local/bin/api-server
            - start
            - --db-host={{.Release.Name}}-postgresql
          ports:
            - name: api-server
              containerPort: 8080
          readinessProbe:
            grpc:
              port: 8080
          livenessProbe:
            grpc:
              port: 8080
        - name: discovery-agent
          image: {{.Values.discoveryAgent.image}}
          imagePullPolicy: Always
          command:
            - /usr/local/bin/discovery-agent
            - start
          readinessProbe:
            grpc:
              port: 8081
          livenessProbe:
            grpc:
              port: 8081



