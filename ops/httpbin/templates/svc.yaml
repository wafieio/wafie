apiVersion: v1
kind: Service
metadata:
  name: httpbin
  namespace: {{.Release.Namespace}}
  labels:
    app: httpbin
spec:
  ports:
    - name: http-ex-spiffe
      port: 443
      targetPort: 8080
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: httpbin