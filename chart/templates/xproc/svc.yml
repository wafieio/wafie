apiVersion: v1
kind: Service
metadata:
  name: wafie-xproc
  namespace: {{.Release.Namespace}}
spec:
  type: ClusterIP
  selector:
    app: wafie-xproc
  ports:
    - name: grpc-server
      port: 50051