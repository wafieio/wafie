
# base host which will be used as a base domain
# for different services
baseHost: ""

# Control Plane API parameters
api:
  image: dimssss/wafie-control-plane:latest
  svc:
    name: wafie-api
    port: 80

# Discover Agent parameters
discoveryAgent:
  image: dimssss/wafie-control-plane:latest

# Application Security Gateway parameters
gateway:
  ads: dimssss/wafie-gateway:latest
  proxy: envoyproxy/envoy:v1.36.2
  xproc: docker.io/dimssss/wafie-xproc:latest

# Relay parameters
relay:
  image: docker.io/dimssss/wafie-relay:latest

# Shared configurations
config:
  apiAddr: ""

# postgresql configurations
postgresql:
  global:
    security:
      allowInsecureImages: true
  image:
    repository: bitnamilegacy/postgresql
  auth:
    postgresPassword: "cwafpg"
    username: "cwafpg"
    password: "cwafpg"
    database: "cwaf"
  primary:
    persistence:
      size: 20Gi
  volumePermissions:
    enabled: false

opensearch:
  singleNode: true
  opensearch.yml: |
    cluster.name: opensearch-cluster
    network.host: 0.0.0.0
  extraEnvs:
    - name: OPENSEARCH_INITIAL_ADMIN_PASSWORD
      value: 8NybEHCU81IzgWhx
  persistence:
    enabled: true
